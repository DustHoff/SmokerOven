plugins {
    id "org.springframework.boot" version "1.5.9.RELEASE" apply false
}

allprojects {
    apply plugin: 'maven'

    group = 'de.onesi.hoffnet'
    version = '1.0-SNAPSHOT'
}

subprojects {
    repositories {
        mavenLocal()
        jcenter()
    }
}